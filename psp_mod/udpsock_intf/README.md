# UDP Socket Interface PSP Module

This is a PSP module intended to be used with the PSP IO driver interface in order to create UDP connections. 

Note that for BPNode, the value of `UDPSOCK_MAX_INTERFACE_DEVS` should always be equivalent to `BPLIB_MAX_NUM_CONTACTS` times 2.
